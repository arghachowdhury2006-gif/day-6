#include<stdio.h>
int main() {
	
	int n, i, a = 0, b = 1, sum;
		printf("Enter a number:");
		scanf("%d", &n);
		for(i = 1; i<=n; i++) {
			sum =  a + b;
		    a = b;
		    b = sum;
		printf("fibonacci series is : %d\n", sum);
}
	
	
	
return 0;	
}
